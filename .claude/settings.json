{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$CLAUDE_TOOL_INPUT\" | python3 -c \"import sys,json; d=json.load(sys.stdin); f=d.get('file_path',''); vendors=['jspdf.min.js','opencv.js']; matched=next((v for v in vendors if v in f), None); print(f'ERROR: {matched} は編集禁止です。更新する場合は公式から再取得してください。') if matched else None; exit(1 if matched else 0)\""
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$CLAUDE_TOOL_INPUT\" | python3 -c \"import sys,json; d=json.load(sys.stdin); f=d.get('file_path',''); print('iOS Safari で動作確認を: カメラ / 台形補正 / Web Share API') if 'index.html' in f else None\""
          }
        ]
      }
    ]
  }
}
